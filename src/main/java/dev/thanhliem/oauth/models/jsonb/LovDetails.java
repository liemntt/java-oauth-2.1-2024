package dev.thanhliem.oauth.models.jsonb;


/**
 * This abstract class serves as a base for all lov entities in the system.
 * It currently does not define any common behavior or properties but exists
 * to provide a common type for all entities.
 */

public interface LovDetails {
    // Intentionally left blank for future use
}
